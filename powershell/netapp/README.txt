#Script to install Netapp C-Mode Filer
Once the Netapp is racked and connected to the network this script will configure all of the required settings to allow the system to
begin serving data. 
Variables are set within the file, and the script will be run from a device that can reach the netapp system.
